{
  "name": "prompt-asset-sound-wav",
  "version": "0.1.0",
  "description": "Proper WAV encoder for prompt-asset-sound",
  "main": "src/index.ts",
  "bin": {
    "prompt-asset-sound": "./src/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
